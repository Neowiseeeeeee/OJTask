node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Never commit secrets
.env
.env.*
!.env.example

# Logs and temp files
seed.log
*.log

# User-uploaded files — never commit these
uploads/
