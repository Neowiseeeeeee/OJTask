# TODO

- [ ] Restart + standardize `messages-page-*` rendering logic (no modal handler) to fix role inconsistencies.
- [ ] Confirm message ordering and sender display fallbacks across roles.
- [ ] Ensure DM sidebar shows only conversation partners with correct names/avatars.
- [ ] Remove/avoid references that cause runtime crashes.

